<nav class="navbar navbar-expand-lg navbar-light fixed-top" style="background-color: #e3f2fd;">
    <div class="container">
    <a class="navbar-brand" href="/">To Do List App</a>
        <a href="/"><img src="{{ asset('img/Logo_OpenClassrooms.png') }}" alt="OpenClassrooms" /></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
        <ul class="navbar-nav mr-auto">
            <li>
                {% if app.user %}
                    <a class="nav-link" href="{{ path('task_list') }}">Ma liste de tâches</a>
                {% endif %}
            </li>
            {% if is_granted('ROLE_ADMIN') %}
            <li>
                <a class="nav-link" href="{{ path('user_list') }}">Gérer les utilisateurs</a>
            </li>
            <li>
                <a class="nav-link" href="{{ path('user_create') }}">Créer un utilisateur</a>
            </li>
            {% endif %}
            <li>
                {% if app.user %}
                    <a style="color: red" class="nav-link" href="{{ path('logout') }}">Se déconnecter</a>
                {% endif %}
            </li>
            <li>
                {% if not app.user and 'login' != app.request.attributes.get('_route') %}
                    <a style="color: green" class="nav-link" href="{{ path('login') }}">Se connecter <i class="fas fa-sign-in-alt"></i></a>
                {% endif %}
            </li>
        </ul>
    </div>
    </div>
</nav>